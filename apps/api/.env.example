DATABASE_URL="postgresql://epg:epg_dev_password@localhost:5432/epg_platform?schema=public"
PORT=4000
NODE_ENV=development
JWT_SECRET="change-me-in-every-environment"
JWT_EXPIRES_IN="8h"
# 32-byte hex key (64 hex chars) for AES-256-GCM at-rest encryption of
# webhook connector secrets. Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY="change-me-64-hex-chars-in-every-environment"
